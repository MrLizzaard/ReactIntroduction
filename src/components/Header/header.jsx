import React from "react";

const Header = (props) => {
  return (
    <div className="header introHeader">
      <div className="backMainBtn">Back to the main</div>
      <div className="aboutBtn">ABOUT</div>
    </div>
  );
};

export default Header;
